// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Dominio de producción: define las URLs canónicas y las etiquetas Open Graph.
  // Los despliegues de preview también apuntan aquí a propósito, que es lo
  // deseable para el canonical.
  site: 'https://horizonsport.co',
});
